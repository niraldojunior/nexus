The base panel surface for everything in the product.

```jsx
<Card interactive pad={24}>
  <h3>CTO-4821</h3>
</Card>
```

Set `interactive` for clickable cards (adds the hover lift + golden glow). `pad` controls inner padding. Compose `MetricCard` for KPIs instead of building them from `Card`.
