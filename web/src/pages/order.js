import { renderShell } from '../shell.js';

export const renderOrder = () => renderShell('order', `
  <section class="page">
    <h1>Order</h1>
    <p class="lead">Página em estrutura para qualification, ordering e fulfillment.</p>
    <div class="card">
      <h2>Próximo passo</h2>
      <p>Conectar as jornadas de viabilidade e execução do fluxo de ordem.</p>
    </div>
  </section>
`);
